<%= simple_form_for @project do |f| %>
  <%= flash[:error] %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :target_value %>
  <%= f.submit "Salvar", class: 'btn btn-primary' %>
<% end %>
